import styled from '@emotion/styled';
import { Cell } from 'shared/styles/main/cell';

export const RandomCell = styled(Cell)`
  svg {
    position: absolute;
    left: 1.25rem;
    bottom: 0;
  }
`;
